# Notas:
Primer programa en Node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Base de la tabla de multiplicar a generar   [number] [required]
  -l, --listar   Muesta la impresión por consola, deshabilitada por defecto
                                                      [boolean] [default: false]
  -m, --maximo   Numero maximo a multiplicar              [number] [default: 10]
```